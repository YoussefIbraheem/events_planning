meta {
  name: login_create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/login/
  body: json
  auth: inherit
}

headers {
  Authorization: Token {{apiKey}}
}

body:json {
  {
    "username": "ioneal",
    "password": "password@123"
  }
}

script:post-response {
  const access_token = res.body.access_token
  bru.setEnvVar("apiKey", access_token)
}
